package EjercicioGeometrico.dominio;

/*public class TrianguloIsosceles extends Triangulo{
    int alturaTIsoc= (int) (Math.pow(this.getHipotenusa(),2)-(Math.pow(this.getBase(),2)/4));
    int resultAlturaIsos= (int) Math.sqrt(alturaTIsoc);

    public TrianguloIsosceles(int base, int hipotenusa, int altura) {
        super(base, hipotenusa, altura);
    }

    public TrianguloIsosceles(int base, int hipotenusa, int altura, int alturaTIsoc, int resultAlturaIsos) {
        super(base, hipotenusa, altura);
        this.alturaTIsoc = alturaTIsoc;
        this.resultAlturaIsos = resultAlturaIsos;
    }

    @Override
    public double calcularArea() {
        return (this.getBase()*this.resultAlturaIsos)/2;
    }

    @Override
    public double calcularPerimetro() {
        return (2*this.getHipotenusa()+this.getBase());
    }

}*/

